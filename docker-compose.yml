services:
  postgres:
    image: postgres:16-alpine
    container_name: imovdigital-postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: imovdigital
      POSTGRES_PASSWORD: imovdigital123
      POSTGRES_DB: imovdigital
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: imovdigital-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:
